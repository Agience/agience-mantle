# Blackbox E2E harness deps — hits the running stack over HTTP only.
# Deliberately does NOT depend on mantle/origin/core packages.
pytest>=8.0
httpx>=0.27
websockets>=12.0
python-jose[cryptography]>=3.3
cryptography>=43.0
