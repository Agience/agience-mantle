# Use: pip install -r requirements.txt

# --- Agience shared platform (trust/protocol floor) ---
# service_identity, authority_trust, key_manager, config, scopes, event_bus, logging.
# `platform` was extracted to agience-origin (formerly agience-core) and is NOT vendored here;
# the code under mantle/ imports it as `platform`.
#   Local dev (until agience-origin is published) — editable install from the sibling checkout:
#     pip install -e ../agience-core/src/agience_core
#   Production — pin to a pushed tag:
#     agience_core @ git+ssh://git@github.com/Agience/agience-origin.git@platform-v0.1.0#subdirectory=src/agience_core

# Authentication & Security
authlib==1.6.9
bcrypt
cryptography==46.0.7
webauthn>=2.5.0

# Email (transactional)
aiosmtplib==3.0.2

# Core Web Framework
fastapi
starlette==0.49.3
itsdangerous
uvicorn[standard]==0.37.0
python-multipart==0.0.26
python-dotenv>=1.0.0

# MCP (Model Context Protocol) - official SDK
mcp

# Data Validation
pydantic==2.12.0
pydantic[email]
pydantic-core==2.41.1
annotated-types==0.7.0
email-validator==2.2.0

# AI & Embeddings
anthropic>=0.42.0
google-genai>=1.0.0
tiktoken==0.12.0

# Vector Database & Search
numpy==2.3.3
# opensearch-py retired in Step 2.6.9 (2026-05-09); search runs on
# encrypted MANTLE + MANTLE-SSE.

# Database

# Relational (Origin — identity, OIDC, grants). SQLite via stdlib sqlite3.
sqlalchemy>=2.0,<3.0
alembic>=1.13

# Manifest loader (Origin)
pyyaml>=6.0

# Graph Database
python-arango==8.1.3

# Cloud Storage
boto3==1.40.50
botocore==1.40.50

# HTTP & Utilities
requests==2.33.1
httpx==0.28.1
anyio==4.11.0
typing-extensions==4.15.0
python-dateutil==2.9.0.post0
python-jose==3.5.0

# Testing
pytest==8.4.2
pytest-asyncio>=1.0.0
pytest-xdist>=3.5.0
pytest-timeout>=2.2.0