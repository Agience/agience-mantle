"""Persistence — the shard, its content, and what is kept.

The SQLite lattice + filesystem CAS, the local cache and its regions, content tiering,
curation and erasure. Nothing here knows what the artifacts mean.
"""
